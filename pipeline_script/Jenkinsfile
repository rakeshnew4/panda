pipeline {
    agent {
        kubernetes {
            yaml """
apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: my-pipeline-pod
spec:
  containers:
  - name: dockerindocker
    image: docker:dind
    tty: true
"""
        }
    }
    stages {
        stage("Deploy") {
            steps {
                container("dockerindocker"){
                sh "docker build -t panda:v1 ."  // Run shell command to list files
                echo "Hello, Jenkins!"  // Print a message
                }

            }
        }
    }
}
