pipeline {
    agent {
        kubernetes {
            yaml """
apiVersion: v1
kind: Pod
metadata:
  labels:
    some-label: my-pipeline-pod
spec:
  containers:
  - name: dockerindocker
    image: docker:1.2.0
    tty: true
"""
        }
    }
    stages {
        stage("Deploy") {
            steps {
                container("dockerindocker"){
                sh "docker --version"  // Run shell command to list files
                echo "Hello, Jenkins!"  // Print a message
                }

            }
        }
    }
}
